version: 0.2
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
phases:
  install:
    commands:
        - echo Installing..
  pre_build:
    commands:
        - echo Prebuild.
  build:
    commands:
        - echo build..
  post_build:
    commands:
        - echo post_build..
